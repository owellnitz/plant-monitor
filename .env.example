# Copy to .env (gitignored) and set a real value:
#   openssl rand -base64 18 | tr '+/' '-_'
POSTGRES_PASSWORD=change-me

# Optional, only for docker-compose.release.yml: pins the backend image
# to a released version, e.g. 1.0.0. Unset = latest release.
#APP_IMAGE_TAG=1.0.0
